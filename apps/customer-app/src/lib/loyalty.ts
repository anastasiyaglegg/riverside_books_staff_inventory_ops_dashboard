// Mirrors apps/backend/lib/loyalty.ts STAMPS_PER_REWARD -- backend flags it as an
// ASSUMPTION pending store-owner confirmation. Keep this in sync if that changes.
export const STAMPS_PER_REWARD = 10;
