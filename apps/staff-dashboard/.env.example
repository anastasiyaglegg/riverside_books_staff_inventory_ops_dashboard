# Public values only -- never put the Supabase service role key or any backend-only
# secret in this app. This is a browser-shipped SPA.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:3000/api/v1
# Elliot's marketing generator frontend (Product D), embedded in the Marketing tab.
# Optional -- the app falls back to the known deployment if unset.
VITE_MARKETING_APP_URL=https://riverside-marketing-content-generat.vercel.app
